@model ERPElectrodata.Models.STATUS_FEEDBACK

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm("SaveCreate", "StatusFeedback", FormMethod.Post, new { id = "idFormcreate", target = "upload_target" }))
{
    
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>STATUS_FEEDBACK</legend>

        
        <div class="editor-label">
            @Html.LabelFor(model => model.NAM_STAT_FEED)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NAM_STAT_FEED)
            @Html.ValidationMessageFor(model => model.NAM_STAT_FEED)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DES_STAT_FEED)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DES_STAT_FEED)
            @Html.ValidationMessageFor(model => model.DES_STAT_FEED)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
